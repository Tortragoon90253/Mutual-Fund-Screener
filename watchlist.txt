# รายชื่อกองทุนสำหรับ "โหมด watchlist" (ตัวเลือกสำรอง)
#
# ค่าเริ่มต้นของระบบคือดึง "ทุกกองที่เปิดขาย" — ไฟล์นี้จะถูกใช้ก็ต่อเมื่อ
# ตั้ง Repository variable ชื่อ DATA_MODE = watchlist (เช่น เมื่อโควตา API ไม่พอสำหรับการดึงทุกกอง)
#
# เขียนได้ 3 แบบ (บรรทัดละ 1 กอง):
#   ชื่อย่อกองทุน          -> ดึงทุกชนิดหน่วยลงทุนของกองนั้น     เช่น  SCBS&P500
#   ชื่อย่อ | ชนิดหน่วย     -> ดึงเฉพาะชนิดที่ระบุ                  เช่น  SCBS&P500 | SCBS&P500A
#   รหัสโครงการ (proj_id)  -> ใช้เมื่อชื่อย่อซ้ำหรือหาไม่เจอ          เช่น  M0123_2560
#
# ถ้าชื่อผิด ระบบจะแจ้งเตือนในหน้า Actions พร้อมชื่อที่ใกล้เคียง แต่กองอื่นยังดึงต่อได้
# ตัวอย่างด้านล่างใส่ไว้ให้ลองระบบเท่านั้น — ไม่ใช่คำแนะนำให้ลงทุน

SCBS&P500
B-INNOTECH
