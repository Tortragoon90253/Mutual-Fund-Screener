# API keys — never commit
sec-config.json
.env

# Python / OS / build leftovers
__pycache__/
*.pyc
_site/
Thumbs.db
.DS_Store
data/
data-branch/

# ไฟล์ตั้งค่าเครื่องมือระหว่างพัฒนา ไม่ใช่ส่วนหนึ่งของโปรแกรม
.claude/
